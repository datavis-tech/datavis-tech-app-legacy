// The id used in Stripe for the Early Adopter plan.
const EARLY_ADOPTER = 'early-adopter'

// The monthly cost in cents USD for the Early Adopter plan.
const EARLY_ADOPTER_COST = 499

// The string to display that represents the cost in USD.
const EARLY_ADOPTER_COST_DISPLAY = '$' + EARLY_ADOPTER_COST / 100

module.exports = {
  EARLY_ADOPTER,
  EARLY_ADOPTER_COST,
  EARLY_ADOPTER_COST_DISPLAY
}
